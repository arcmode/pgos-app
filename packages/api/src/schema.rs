table! {
    orders (order_) {
        order_ -> Integer,
        coffee -> Text,
        method_ -> Text,
        number_of_cases -> Integer,
        packets_per_case -> Integer,
        ship_date -> Date,
        priority -> Bool,
    }
}
